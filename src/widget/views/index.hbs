<svg xmlns="http://www.w3.org/2000/svg" width="800" height="{{ height }}" viewBox="0 0 800 {{ height }}">
    <style>
        text {
            font-weight: normal;
            transform: translateX(5px);
        }
    </style>
    <rect width="800" height="{{ height_ }}" style="fill: {{background_color}};" rx="5" />
    <text y="20" font-family="Consolas, monospace" fill="{{main_color}}">
        {{{json}}}
    </text>
</svg>